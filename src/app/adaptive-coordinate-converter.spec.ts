import { AdaptiveCoordinateConverter } from './adaptive-coordinate-converter';

describe('AdaptiveCoordinateConverter', () => {
  it('should create an instance', () => {
    expect(new AdaptiveCoordinateConverter()).toBeTruthy();
  });
});
